# Make URL from ML number

Make URL from ML number

## Usage

``` r
format_ML_link(results_tbl, ML_vector = `ML Number`)
```

## Arguments

- results_tbl:

  tibble containing search data

- ML_vector:

  The vector name for the document ML number

## Value

tibble with new link variable containing an ML URL
